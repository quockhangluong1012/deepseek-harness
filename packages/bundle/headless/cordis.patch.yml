# The dsh-headless bundle patch: one-shot task mode directly over dsh-base.
# It mounts no Host, HTTP server, Web runtime, or browser plugin. An ordinary
# provider plugin injects `cmdlineArgs`, parses the task positional
# (`dsh --profile headless "<task>"`), the `--session-id` and `--json` options,
# and this app's --help, then the direct driver creates a fresh Agent, or adopts
# the exact Session the flag names, through the core registry and prints its
# durable result.

- id: system-prompt
  config:
    personaSuffix: Your working directory is {{cwd}}.
    personaPrefix: >-
      You are a coding agent powered by the {{model}} model.

- id: tools
  config:
    # Keep the same temporary process-wide PTC mode opt-in as the Web surface.
    mode: !!js process.env.DSH_TOOLS_MODE

- insert:
    # Governance plane (spec §33 B0), mirroring the web surface: the kernel
    # and its builtin capability declarations, plus the prompt-injection
    # guard, all mounted in `shadow`. Shadow records what each decision would
    # have been and changes no behaviour.
    - id: agent-kernel
      name: '@deepseek-ai/dsh-agent-kernel'
      config:
        mode: shadow

    - id: agent-kernel-builtins
      name: '@deepseek-ai/dsh-agent-kernel-builtins'

    - id: prompt-injection
      name: '@deepseek-ai/dsh-prompt-injection'
      config:
        mode: shadow

    - id: headless-startup
      name: '@deepseek-ai/dsh-headless/startup'

    # Reads its task and every run option from the ordinary headlessStartup
    # provider: the resume/model/permission/turn/prompt/tool flags below all
    # parse and validate at startup, and reach the run only through here.
    - id: headless-runner
      name: '@deepseek-ai/dsh-headless'
      inject: [headlessStartup]
      config:
        task: !!js ctx.headlessStartup.task
        sessionId: !!js ctx.headlessStartup.sessionId
        json: !!js ctx.headlessStartup.json
        continueLatest: !!js ctx.headlessStartup.continueLatest
        model: !!js ctx.headlessStartup.model
        permissionMode: !!js ctx.headlessStartup.permissionMode
        maxTurns: !!js ctx.headlessStartup.maxTurns
        systemPrompt: !!js ctx.headlessStartup.systemPrompt
        allowedTools: !!js ctx.headlessStartup.allowedTools
        outputSchema: !!js ctx.headlessStartup.outputSchema

- id: hmr
  disabled: true
