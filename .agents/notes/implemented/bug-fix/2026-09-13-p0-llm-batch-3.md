# P0-llm fixes batch 3

- Date: 2026-09-13
- Batch: 3 of full-sweep fixes (`docs/superpowers/specs/2026-09-13-full-sweep-fixes-design.md`)

## Code changes (3 files)

1. `llm-retry/src/index.ts`: four bare `return` abort paths now return
   `undefined` explicitly with one-line comments naming the declined condition.
   Zero behavior change; existing cancellation tests (retry.spec.ts:953, 976)
   pin the semantics.
2. `llm/src/assembler.ts`: class JSDoc records the layering contract —
   `llm-invariant` strictly throws on malformed streams when mounted, the
   assembler stays total for compositions running without it. Comment only.
3. `command-goal`: local `assertNever` replaced with the shared
   `@deepseek-ai/dsh-util-values` import (plus `dependencies` entry and
   `tsconfig` reference). The local message format is superseded by the shared
   `unreachable variant in <context>` format; no test pinned the old text.

## Triaged without code change (with reasons)

- **`always` swallowing downstream decisions: refuted.** `RequestErrorAction`
  is the closed union `{ kind: 'retry' } | undefined`
  (`core/agent/src/runtime-types.ts:122`): no fail/abort kinds exist for the
  finding to bite on. Decline-then-retry is the designed composition, pinned
  by `retry.spec.ts:728,751`, and the llm-fallback decision
  (`.agents/notes/implemented/feature/2026-09-11-llm-fallback.md:25`)
  records always-starves-fallback as an accepted trade-off.
- **`always` unbounded: already documented.** The policy interface, the
  `llm-retry` README (cost ownership + `llm/retry` metric), and the
  `keeps always mode unbounded` test all agree. No change.
- **translate empty id/name: pinned tolerance.** `translate.spec.ts:331`
  deliberately pins empty-string fallbacks for lenient gateways, and an empty
  name fails LOUD at tool dispatch (unknown tool), never silently. Strictness
  would regress gateway compat. No change.
- **quanta `?? 1`: accepted trade-off.** All production constructors pass
  quanta explicitly or take the identity default; the pure function documents
  determinism. An interface churn across 3 providers + tests + `.d.ts`
  mirrors buys nothing observable. No change.
- **`dsh-foreign` replay default: deliberate.** `foreignAssistant`
  (`llm-pi-ai/src/replay.ts:153`) exists to convert history WITHOUT trusting
  it as same-model replay; throwing would break cross-provider replay. No change.
- **SSE `block-end` batching: deferred to batch 7.** Incremental emission is a
  redesign needing TTFT measurement, not a bug fix. Moved to P1 with a
  measurement requirement.
- **process-local breaker: already documented.** Module JSDoc, README body,
  and `## Known Limitations and Deferred Work` ("Process-local breaker")
  all state the scope. No change.

## Verification

- `vitest`: `llm/tests/content.spec.ts`, `llm/tests/assembler.spec.ts`,
  `llm-retry/tests/retry.spec.ts` + `invariant.spec.ts`,
  `command-goal/tests/command-goal.spec.ts` — 118/118 pass.
- `tsc -b packages/goal/command-goal/tsconfig.json` — clean.
- `pnpm install` relinked the new workspace dep
  (`command-goal/node_modules/@deepseek-ai/dsh-util-values` present).
