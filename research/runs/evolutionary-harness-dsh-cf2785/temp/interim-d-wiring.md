# Interim D — trajectory + scorer + composition + brief + telemetry (loci L3, L4, L6)

## Findings (from investigator D, verified by orchestrator: trajectory head, scorer head, memory-context index, web-app rows, telemetry README, reviewer README)

- Trajectory `ctx.evolutionTrajectory` (TypertRemoteService): exportSession (flush live session, session/not-found when absent, empty array when no admitted message) + exportScope (profile:workspaceId split, workspace/not-found, non-archived roster sessions, per-log skip+warn). Default $DSH_HOME/evolution-trajectories, outDir configurable; sanitized file names; overwrite-not-append; never inside project; never touches model requests. ShareGPT shaping pure toShareGpt: one conversation per turn with admitted message (<sessionId>#<turn>); vocab system/human/gpt/tool; admission = reviewer rule (user kind=user only); text blocks only (reasoning/image/file dropped); assistant tool-calls rendered as <tool_call> tags; empty messages + pre-first-turn/start events dropped.
- Scorer `ctx.evolutionScorer`: manual baseline runs only (never per-turn). Config corpusDir required + attempts 3. loadScenarioPlan reads input.json + top fixture + children + replay.override.json + workspace/expected; missing → skipped. Metric triple: pass (workspace diff all-attempts-final vs expected), billed tokens (meter over harvested logs incl. nested), median wall time. processScenarioRunner = snapshot-harness ACP tier; keyless replay, never records. Limits: replay-tier/ACP-runner only, host wall-clock, whole-run tokens, no Pareto/GEPA yet.
- Composition: evolution rows ONLY in web-app cordis.patch.yml:102-146 (memory capacityBytes 131072; reviewer profile default; memory-context maxBytes 16384 profile default; telemetry; skill-manage; curator; command-evolution profile default; trajectory; controller profile default). Reviewer+injector share profile namespace; telemetry precedes resolvers. Required: memory.capacityBytes, memory-context.maxBytes, controller.profile, scorer.corpusDir. Disabling = removing rows (no records/brief/extraction, sidebar falls back). Web-only first; headless/sdk/acp byte-identical.
- Brief injector (agent/pre-step, observe-final-batch, spread preserves startsRequestSeries): membership via registry sessionIds → canonical-cwd fallback, cached, disposed on session/disposed. Triple digest gate (in-memory mark → claimed batch → logged surface via readSurface); absent record digest 'empty'; hasContent false → nothing (zero tokens). Budget maxBytes incl. frame: drop trailing Context → truncate Lessons → Profile (lessons gone) → Instructions last + notice line, UTF-8 safe. Files re-read at injection (fs else node:fs); missing → unavailable line, step proceeds. Recall: single 'Recall: <sessionId>' item rendered last (drops first). Frame escape <\/system-reminder>. Nudges: memory every 1 turn, skills every 10th turn & only when skill_manage visible, session-search always; turn counting from turn/start events.
- Telemetry (evolution_skill_usage v1 per-record): use/view/patch counts, createdBy agent only via markAgentCreated on background_review origin; foreground stays user-directed; passive tools/post-execute counting; bundled/hub* excluded. skillCreationEvidence: repeated normalized paths ≥3 → propose skill (no content read). Manage skill_manage: create/patch(edit/write_file/remove_file)/delete over createDir ($DSH_HOME/skills, ~/${VAR} expanded); unique-substring patch; pin blocks delete not patch; bundled/hub refused; mutations reported to telemetry.
- Subsystem page docs/subsystems/evolutionary-harness.md: scope def, Parts table (Outputs/Staged off-model + off-capacity), Packages table.

## Committed position

**Evolutionary harness là ba vòng thời gian lồng nhau, không phải một pipeline.** Vòng turn (inject→act→index→maybe-extract), vòng ngày/tuần (journey read-model + approvals), vòng tháng/quý (curator lifecycle + trajectory/scorer outer loop). Mỗi vòng có trigger, store, và consumer riêng; chỉ brief digest và record stamps nối chúng lại. (Serves L3, L4, L6.)
