# Interim C — curator + controller + command-evolution/journey (loci L2, L5, L6)

## Findings (from investigator C, verified by orchestrator: journey.ts full, command index head, curator head)

- Curator: host-wide single timer tickMinutes 15, start-time due-check, maybeRun needs enabled + interval 168h elapsed + idle 2h (host with no activity counts idle); first call seeds lastRunAt + defers (index.ts:301-322, 430-442). Transitions active→stale 30d→archived 90d via idleMs from lastUsedAt??createdAt; skips pinned/protected/hub(+bundled when pruneBuiltins); dryRun previews; archiveAfterDays<staleAfterDays fails loud (index.ts:264-268, 359-396). Consolidation opt-in (consolidate:false): needs provider+model, survey createdBy==agent, cost ledger row, bounded in-package loop maxSteps 4 over 2-tool whitelist (view/apply), full-package rule (consolidate.ts, index.ts:459-554). Backups tar.gz keep 5, JSONL ledger content-addressed, rollbackPass/entry reversible, adopt manual, purge only archived past TTL (default 0 = never), pin blocks transitions + delete.
- Controller: evolution Remote verbs read/setInstructions/setLessons/setProfile/addContextItem/removeContextItem/rebuildMemory/listStaged/approveStaged/rejectStaged/timeline/follow; scope-first requireWorkspace (workspace/not-found); staged decisions scope-checked (staged-not-found); profile required no-colon. Follow: baseline all registered scopes + ordered upserts from domain/changed filtered to evolution_memory.records non-deleted, recomputed scope keys (other profiles/global ignored). scopeTimeline imported from command-evolution (read model lives once). rebuildMemory needs reviewer else extraction-failed. File contexts resolved + canonicalized inside workspace with observed sizeBytes.
- Commands: /memory pending|approve|reject (skill-kind redirected to /skills); /skills pending|approve (approve drops only after skill_manage wrote file); /journey [today|7d|30d|all] default 7d + /journey export (timeline.json + session-log.jsonl → zip, default $DSH_HOME/exports); /curator status|run|adopt|purge|rollback|ledger|pin|unpin; /refine → reviewer.rebuild immediate; /trajectory [--out] [--all] → exporter; /learn builds prompt + ordinary turn; /suggestions lists blueprint skills, never schedules.
- Journey (pure read model, journey.ts:1-15): TimelineInput{record,usedBytes,capacityBytes,digest,range,now}; recordDeltas ascending: instructions stamp; lessons/profile stamps (fallback: lastExtraction→one lessons delta; else memoryUpdatedAt→hand-edit); per contextItems.addedAt; per outputs.at (tool+path+session); per staged.createdAt; extraction via extraction.at match else hand-edit; resolutions counted per decision day; buckets by UTC+7 dayKey, bounded zero-filled, all sparse; cumulative independent of range; pending from record.staged; renderTimeline one line per active day + capacity + staged hint.

## Committed position

**Timeline không được "ghi" bao giờ — nó được suy ra mỗi lần đọc.** Mọi write-path (instructions/lessons/profile/context/outputs/staged/resolutions) đều thuộc store/reviewer; journey và controller chỉ project chúng lên lịch. Ai tìm "bảng timeline" trong storage sẽ không thấy gì. (Serves L2; governance split serves L5.)
