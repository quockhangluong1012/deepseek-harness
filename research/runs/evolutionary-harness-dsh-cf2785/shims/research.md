## Run directives

Auto-selected for this run in step 1; binding wherever they adjust a default in your prompt. Absent instructions here leave your prompt's defaults untouched.

- register: teach
- inference depth: standard

### Domain notes

Sourcing: repo source files first (packages/evolution/*/src + tests + README + cordis.yml/profile wiring); tests outrank prose docs on trigger semantics. Evidence norm: claim about 'khi nao trigger' must cite the exact condition/call-site in code. Recency window: current working-tree state only.

### Inference depth

Standard depth (this run confirms your prompt's defaults). Follow the
normal sourcing playbook.
