verify-translation-pairing: bilingual pairing rules violated (see docs/i18n/README.md):
  .agents/notes/implemented/architecture/2026-07-24-web-config-tree-boot-and-transport-layering.md: out of sync — content no longer matches the pair's last confirmed-consistent state in .agents/notes/implemented/architecture/2026-07-24-web-config-tree-boot-and-transport-layering.i18n.yaml (bring the other side along, then re-record with --write)
  .agents/notes/implemented/architecture/2026-07-24-web-config-tree-boot-and-transport-layering.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in .agents/notes/implemented/architecture/2026-07-24-web-config-tree-boot-and-transport-layering.i18n.yaml (bring the other side along, then re-record with --write)
  .agents/notes/implemented/feature/2026-09-12-guard-budgets.md: out of sync — content no longer matches the pair's last confirmed-consistent state in .agents/notes/implemented/feature/2026-09-12-guard-budgets.i18n.yaml (bring the other side along, then re-record with --write)
  apps/cli/reference/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in apps/cli/reference/README.i18n.yaml (bring the other side along, then re-record with --write)
  apps/cli/reference/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in apps/cli/reference/README.i18n.yaml (bring the other side along, then re-record with --write)
  apps/desktop/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in apps/desktop/README.i18n.yaml (bring the other side along, then re-record with --write)
  apps/desktop/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in apps/desktop/README.i18n.yaml (bring the other side along, then re-record with --write)
  docs/architecture.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/architecture.i18n.yaml (bring the other side along, then re-record with --write)
  docs/architecture.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/architecture.i18n.yaml (bring the other side along, then re-record with --write)
  docs/capability-seams.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/capability-seams.i18n.yaml (bring the other side along, then re-record with --write)
  docs/capability-seams.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/capability-seams.i18n.yaml (bring the other side along, then re-record with --write)
  docs/config-catalog.md ↔ docs/config-catalog.zh.md: code block #6 diverges between the pair: "```ts config-catalog\n/**\n * Plugin configuration. Every field is opti… vs "```ts config-catalog\n/**\n * Plugin configuration. Every field is opti…
  docs/config-catalog.md ↔ docs/config-catalog.zh.md: list (kind, start, item count) #1 diverges between the pair: "bullet:items=119" vs "bullet:items=114"
  docs/config-catalog.md ↔ docs/config-catalog.zh.md: link target #113 diverges between the pair: "../packages/llm/llm/src/index.ts" vs "../packages/llm/llm-deepseek/src/config.ts"
  docs/event-producer-consumer.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/event-producer-consumer.i18n.yaml (bring the other side along, then re-record with --write)
  docs/event-producer-consumer.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/event-producer-consumer.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/README.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/README.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/session.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/session.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/session.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/session.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/spill.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/spill.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/webhook.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/webhook.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/webhook.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/webhook.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/workspace-memory.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/workspace-memory.i18n.yaml (bring the other side along, then re-record with --write)
  docs/subsystems/workspace-memory.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/subsystems/workspace-memory.i18n.yaml (bring the other side along, then re-record with --write)
  docs/tool-catalog.md ↔ docs/tool-catalog.zh.md: heading (depth) #45 diverges between the pair: 3 vs 2
  docs/tool-catalog.md ↔ docs/tool-catalog.zh.md: code block #24 diverges between the pair: "```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"patch\"… vs "```json\n{\n  \"type\": \"object\",\n  \"properties\": {\n    \"file_pa…
  docs/tool-catalog.md ↔ docs/tool-catalog.zh.md: table (row x column count) #1 diverges between the pair: "34x6" vs "32x6"
  docs/tool-catalog.md ↔ docs/tool-catalog.zh.md: link target #31 diverges between the pair: "../packages/fs/tool-fs/src/index.ts" vs "../packages/fs/tool-fs-search/src/index.ts"
  docs/user/guide/providers.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/user/guide/providers.i18n.yaml (bring the other side along, then re-record with --write)
  docs/user/guide/providers.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in docs/user/guide/providers.i18n.yaml (bring the other side along, then re-record with --write)
  packages/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/api/gateway/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/api/gateway/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/api/gateway/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/api/gateway/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/bundle/base/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/bundle/base/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/bundle/base/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/bundle/base/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/client/ui-workspace-memory/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/client/ui-workspace-memory/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/client/ui-workspace-memory/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/client/ui-workspace-memory/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/context/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/context/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/context/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/context/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/context/working-set/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/context/working-set/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/core/agent-loop/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/core/agent-loop/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/deliverables/workspace-changes/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/deliverables/workspace-changes/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/deliverables/workspace-changes/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/deliverables/workspace-changes/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/command-evolution/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/command-evolution/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/command-evolution/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/command-evolution/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-benchmark/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-benchmark/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-controller/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-controller/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-curator/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-curator/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-curator/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-curator/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-graph/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-graph/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-graph/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-graph/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-heartbeat/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-heartbeat/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-heartbeat/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-heartbeat/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-lineage/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-lineage/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-lineage/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-lineage/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-metrics/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-metrics/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-operators/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-operators/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-reviewer/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-reviewer/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/evolution/evolution-reviewer/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/evolution/evolution-reviewer/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/fs/tool-fs/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/fs/tool-fs/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/git/tool-git/README.md: incomplete pair — missing packages/git/tool-git/README.i18n.yaml (pairs merge whole: both languages plus the .i18n.yaml record)
  packages/interaction/command-inspect/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/interaction/command-inspect/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/interaction/command-inspect/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/interaction/command-inspect/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/interaction/commands/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/interaction/commands/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/jobs/tool-jobs/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/jobs/tool-jobs/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/jobs/tool-jobs/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/jobs/tool-jobs/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/llm/embeddings-http/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/llm/embeddings-http/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/llm/embeddings-http/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/llm/embeddings-http/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/llm/embeddings/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/llm/embeddings/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/llm/llm-deepseek/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/llm/llm-deepseek/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/llm/llm-deepseek/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/llm/llm-deepseek/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/mcp/mcp-client/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/mcp/mcp-client/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/mcp/mcp-client/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/mcp/mcp-client/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/mentor/learner-model/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/mentor/learner-model/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/mentor/mentor-loop/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/mentor/mentor-loop/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/mentor/misconception/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/mentor/misconception/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/repo/repo-index/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/repo/repo-index/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/research/case-store/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/research/case-store/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/research/case-store/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/research/case-store/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/research/research-controller/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/research/research-controller/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/runtime/agent-context/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/runtime/agent-context/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/runtime/agent-kernel/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/runtime/agent-kernel/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/session/session-format-v4-to-v5/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/session/session-format-v4-to-v5/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/skill/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/skill/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/skill/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/skill/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/skill/evolution-skill-telemetry/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/skill/evolution-skill-telemetry/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/spill/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/spill/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/spill/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/spill/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/spill/spill-local/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/spill/spill-local/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/spill/spill-local/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/spill/spill-local/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/spill/spill/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/spill/spill/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/spill/spill/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/spill/spill/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/subagent/tool-subagent/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/subagent/tool-subagent/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/subagent/tool-subagent/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/subagent/tool-subagent/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/verification/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/verification/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/verification/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/verification/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/verification/agent-verifiers/README.md: incomplete pair — missing packages/verification/agent-verifiers/README.i18n.yaml (pairs merge whole: both languages plus the .i18n.yaml record)
  packages/verification/command-verifiers/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/verification/command-verifiers/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/verification/command-verifiers/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/verification/command-verifiers/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/webhook/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/webhook/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/webhook/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/webhook/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/webhook/webhook-github/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/webhook/webhook-github/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/webhook/webhook/README.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/webhook/webhook/README.i18n.yaml (bring the other side along, then re-record with --write)
  packages/webhook/webhook/README.zh.md: out of sync — content no longer matches the pair's last confirmed-consistent state in packages/webhook/webhook/README.i18n.yaml (bring the other side along, then re-record with --write)
